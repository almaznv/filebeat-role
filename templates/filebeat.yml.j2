output.elasticsearch:
  hosts: ["http://{{ hostvars['el-instance']['ansible_host'] }}:9200"]
  ssl:
    enabled: false
setup.kibana:
  host: "{{ hostvars['kibana-instance']['ansible_host'] }}:5601"
filebeat.config.modules.path: "${path.config}/modules.d/*.yml"